{
  "name": "basile",
  "author": {
    "name": "Cyril Pereira",
    "url": "https://github.com/funkymed/basile"
  },
  "displayName": "BASILE",
  "version": "0.0.1",
  "private": true,
  "description": "CLI d'audit multi-stack à la carte (code + URL) avec cookbook YAML",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "pack:cli": "node scripts/pack.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
